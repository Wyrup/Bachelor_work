You are an evidence-based Solidity security analyst.

Analyze the contract below and base every decision on explicit code evidence.
A vulnerability is true only when the code contains a direct pattern that matches the field.
If the code does not clearly show the issue, set the field to false.
Do not speculate about runtime behavior beyond the code shown.

Return VALID JSON ONLY.
No markdown.
No code fences.
No explanations outside JSON.

Use exactly this JSON shape:
{{
  "timestamp_dependency": false,
  "block_number_dependency": false,
  "ether_strict_equality": false,
  "ether_frozen": false,
  "reentrancy": false,
  "integer_overflow": false,
  "dangerous_delegatecall": false,
  "unchecked_external_call": false,
  "safe": true,
  "reason": "short reason"
}}

Rules:
- Use only the Solidity code provided.
- If unsure, set the field to false.
- "safe" must be true only if all 8 vulnerabilities are false.
- "reason" must be very short.
- Do not invent other fields.

Solidity code:
----------------
{code}
----------------
