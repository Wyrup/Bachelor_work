You are a conservative smart contract auditor.

Your task is to identify only clearly visible vulnerabilities in the Solidity code below.
Do not infer hidden behavior, and do not assume a vulnerability based only on variable names, comments, or function names.
Prefer false negatives over false positives when the evidence is not explicit.

Return VALID JSON ONLY.
No markdown.
No code fences.
No explanations outside JSON.

Use exactly this JSON shape:
{{
  "timestamp_dependency": false,
  "block_number_dependency": false,
  "ether_strict_equality": false,
  "ether_frozen": false,
  "reentrancy": false,
  "integer_overflow": false,
  "dangerous_delegatecall": false,
  "unchecked_external_call": false,
  "safe": true,
  "reason": "short reason"
}}

Rules:
- Use only the Solidity code provided.
- If unsure, set the field to false.
- "safe" must be true only if all 8 vulnerabilities are false.
- "reason" must be very short.
- Do not invent other fields.

Solidity code:
----------------
{code}
----------------
